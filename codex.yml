server:
  port: 8085

spring:
  application:
    name: codex

  datasource:
    url: jdbc:mysql://localhost:3306/codex
    username: root
    password: Cosmo@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
