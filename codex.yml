server:
  port: 8086

spring:
  application:
    name: codex

  datasource:
    url: jdbc:mysql://mysql:3306/codex
    username: root
    password: Cosmo@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/
